import { supabaseClient } from './assets/js/supabase-client.js';
const loginBox=document.querySelector('#loginBox'),app=document.querySelector('#app'),loginForm=document.querySelector('#loginForm'),googleLogin=document.querySelector('#googleLogin'),loginMsg=document.querySelector('#loginMsg'),logout=document.querySelector('#logout'),days=document.querySelector('#days'),refresh=document.querySelector('#refresh');
function setText(id,v){const e=document.getElementById(id);if(e)e.textContent=v}
function esc(v){return String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;')}
function pct(n,d){return d?`${Math.round((Number(n)||0)/(Number(d)||1)*100)}%`:'0%'}
async function load(){refresh.disabled=true;refresh.textContent='Đang tải…';const {data,error}=await supabaseClient.rpc('admin_growth_retention_v1',{p_days:Number(days.value)||30});refresh.disabled=false;refresh.textContent='Làm mới';if(error){document.querySelector('#toolTable').innerHTML=`<div class="note">Không tải được: ${esc(error.message)}</div>`;return}const s=data?.sessions||{},c=data?.conversions||{};setText('mTotal',s.total??0);setText('mReturn2',`${s.returning_2d??0} · ${pct(s.returning_2d,s.total)}`);setText('mMulti',`${s.multi_tool??0} · ${pct(s.multi_tool,s.total)}`);setText('mNorth',`${s.north_star??0} · ${pct(s.north_star,s.total)}`);setText('mLeads',c.market_leads??0);setText('cMeeting',c.meetings??0);setText('cAfter',c.after_session_saved??0);setText('cWatch',c.watchlist_items??0);setText('cLead',c.market_leads??0);
 const tools=data?.tool_usage||[];document.querySelector('#toolTable').innerHTML=tools.length?`<div class="row head"><span>Công cụ</span><span>Session</span><span>Lượt</span><span>Hành động</span></div>${tools.map(x=>`<div class="row"><strong>${esc(x.tool_code)}</strong><span>${x.sessions}</span><span>${x.uses}</span><span>${x.actions}</span></div>`).join('')}`:'<div class="note">Chưa có dữ liệu.</div>';
 const daily=data?.daily||[];document.querySelector('#dailyBox').innerHTML=daily.length?daily.map(x=>`<div class="day"><strong>${esc(x.event_day)}</strong><span>Session ${x.sessions}</span><span>Market ${x.market_reader}</span><span>Sau phiên ${x.after_session}</span><span>Watchlist ${x.watchlist}</span><span>Nhật ký ${x.journal}</span></div>`).join(''):'<div class="note">Chưa có dữ liệu theo ngày.</div>'}
async function showApp(){loginBox.classList.add('hidden');app.classList.remove('hidden');logout.classList.remove('hidden');await load()}function showLogin(){loginBox.classList.remove('hidden');app.classList.add('hidden');logout.classList.add('hidden')}
loginForm.addEventListener('submit',async e=>{e.preventDefault();loginMsg.textContent='Đang đăng nhập…';const {error}=await supabaseClient.auth.signInWithPassword({email:document.querySelector('#email').value.trim(),password:document.querySelector('#password').value});if(error){loginMsg.textContent='Không đăng nhập được. Có thể dùng Google.';return}loginMsg.textContent='';await showApp()});googleLogin?.addEventListener('click',async()=>{const redirectTo=window.location.origin+window.location.pathname;const {error}=await supabaseClient.auth.signInWithOAuth({provider:'google',options:{redirectTo,queryParams:{prompt:'select_account'}}});if(error)loginMsg.textContent=error.message});logout.addEventListener('click',async()=>{await supabaseClient.auth.signOut();showLogin()});days.addEventListener('change',load);refresh.addEventListener('click',load);const {data}=await supabaseClient.auth.getSession();data.session?showApp():showLogin();