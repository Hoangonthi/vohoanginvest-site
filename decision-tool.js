import { supabaseClient } from './assets/js/supabase-client.js';
const $=s=>document.querySelector(s); const stage=document.body.dataset.stage; const fmt=v=>new Intl.NumberFormat('vi-VN',{maximumFractionDigits:1}).format(Number(v||0));
async function ensureAuth(){const {data}=await supabaseClient.auth.getSession();if(!data.session){location.href='investor-os.html';return false}return true}
async function save(stage,type,input,output,next,quality){await supabaseClient.rpc('investor_record_decision_v1',{p_stage:stage,p_type:type,p_input:input,p_output:output,p_next_best_action:next,p_decision_quality:quality,p_position_id:null});}
function show(){const r=$('#result');if(r){r.classList.add('show');r.scrollIntoView({behavior:'smooth',block:'start'})}}
function setMsg(t){const m=$('#msg');if(m)m.textContent=t}
async function runPrebuy(){const input={symbol:$('#symbol').value.trim().toUpperCase(),price:$('#price').value,stop_price:$('#stopPrice').value,desired_amount:$('#desiredAmount').value,thesis:$('#thesis').value.trim()};if(!input.symbol||!input.price||!input.stop_price){setMsg('Nhập mã, giá mua và giá stop.');return}const {data,error}=await supabaseClient.rpc('investor_prebuy_check_v1',{p_payload:input});if(error){setMsg(error.message==='PASSPORT_REQUIRED'?'Hãy hoàn thiện Investor Passport trước.':`Chưa tính được: ${error.message}`);return}$('#rRisk').textContent=`${fmt(data.risk_budget)} đ`;$('#rAmount').textContent=`${fmt(data.allowed_amount)} đ`;$('#rPct').textContent=`${fmt(data.position_pct)}%`;$('#rAction').textContent=data.next_best_action;show()}
async function runHolding(){const avg=Number($('#avgPrice').value),cur=Number($('#marketPrice').value),stop=Number($('#stopPrice').value),valid=$('#thesisValid').value,mat=$('#materiality').value;const pnl=avg?((cur/avg-1)*100):0;let next='Tiếp tục giữ nhưng chỉ khi thesis và stop vẫn còn hiệu lực.';let q=75;if(valid==='NO'){next='Thesis đã sai: ưu tiên giảm/đóng vị thế thay vì chờ giá quay lại.';q=90}else if(valid==='UNCERTAIN'||mat==='HIGH'){next='Không tăng thêm vị thế. Cập nhật thesis bằng dữ kiện mới trước khi quyết định.';q=80}else if(stop&&cur<=stop){next='Giá đã chạm/vượt mức stop đã đặt. Thực hiện rule hoặc ghi rõ lý do nếu thay đổi rule.';q=88}const input={symbol:$('#symbol').value.trim().toUpperCase(),avg_price:avg,market_price:cur,stop_price:stop,thesis_valid:valid,materiality:mat,new_info:$('#newInfo').value.trim()};const output={pnl_pct:pnl,thesis_valid:valid,materiality:mat};await save('HOLDING','THESIS_REVIEW',input,output,next,q);$('#rPnl').textContent=`${fmt(pnl)}%`;$('#rThesis').textContent=valid==='YES'?'Còn đúng':valid==='NO'?'Đã sai':'Chưa chắc';$('#rMat').textContent=mat;$('#rAction').textContent=next;show()}
async function runEvent(){const rel=$('#reliability').value,mat=$('#materiality').value,move=Math.abs(Number($('#movePct').value||0)),exp=Number($('#exposure').value||0);let emotion='Thấp',next='Ghi nhận thông tin, chưa cần thay đổi vị thế.';let q=78;if(rel==='LOW'&&mat==='LOW'){next='Không hành động chỉ vì tin này. Chờ xác minh từ nguồn đáng tin hơn.';emotion=move>=5?'Cao':'Trung bình'}else if(mat==='HIGH'&&rel==='HIGH'){next=exp>30?'Rà soát thesis ngay và cân nhắc giảm exposure nếu dữ kiện mới làm luận điểm sai.':'Cập nhật thesis và xác định lại mức risk trước khi tăng/giảm vị thế.';emotion='Trung bình';q=88}else if(move>=7&&rel!=='HIGH'){next='Giá biến động mạnh nhưng dữ kiện chưa đủ tin cậy. Tránh panic/FOMO; ưu tiên rule đã có.';emotion='Cao'}const input={symbol:$('#symbol').value.trim().toUpperCase(),source:$('#source').value,reliability:rel,materiality:mat,exposure_pct:exp,move_pct:Number($('#movePct').value||0),event:$('#eventText').value.trim()};const output={emotion_risk:emotion,reliability:rel,materiality:mat};await save('EVENT','EVENT_TRIAGE',input,output,next,q);$('#rRel').textContent=rel;$('#rMat').textContent=mat;$('#rEmotion').textContent=emotion;$('#rAction').textContent=next;show()}
async function runPostSell(){const entry=Number($('#entry').value),exit=Number($('#exit').value),ret=entry?((exit/entry-1)*100):0,rule=$('#ruleFollowed').value,reason=$('#sellReason').value;let dq=rule==='YES'?88:rule==='PARTLY'?65:35; if(reason==='PANIC'||reason==='FOMO')dq-=10;dq=Math.max(0,Math.min(100,dq));let next=dq>=80?'Giữ lại rule đã giúp quyết định này có chất lượng và tái sử dụng trước lệnh sau.':dq>=60?'Ghi rõ phần rule đã bị thay đổi và điều kiện để lần sau không lặp lại.':'Review lại lệnh này trước khi mở vị thế mới; ưu tiên sửa quy trình hơn là tìm mã mới.';const input={symbol:$('#symbol').value.trim().toUpperCase(),entry,exit,planned_stop:Number($('#plannedStop').value||0),sell_reason:reason,rule_followed:rule,lesson:$('#lesson').value.trim()};const output={return_pct:ret,decision_quality:dq};await save('POST_SELL','TRADE_REVIEW',input,output,next,dq);$('#rReturn').textContent=`${fmt(ret)}%`;$('#rDecision').textContent=`${dq}/100`;$('#rRule').textContent=rule;$('#rAction').textContent=next;show()}
async function runWeekly(){const {data,error}=await supabaseClient.rpc('investor_get_decision_history_v1',{p_limit:50});if(error){setMsg(error.message);return}const rows=data||[],weekAgo=Date.now()-7*86400000,recent=rows.filter(r=>new Date(r.created_at).getTime()>=weekAgo);const pre=recent.filter(r=>r.decision_stage==='PRE_BUY').length,post=recent.filter(r=>r.decision_stage==='POST_SELL').length,event=recent.filter(r=>r.decision_stage==='EVENT').length;let pattern='Chưa đủ dữ liệu để kết luận pattern. Hãy tiếp tục dùng tool trước và sau quyết định.';let next='Tuần tới: dùng tool Trước mua cho mọi vị thế mới và Review sau bán cho mọi lệnh đóng.';if(pre>=3&&post===0){pattern='Anh kiểm tra khá nhiều trước mua nhưng gần như chưa review sau bán. Hệ thống đang thiếu vòng phản hồi.';next='Bắt buộc review ít nhất 1 lệnh đã bán để tránh học sai chỉ từ P&L.'}else if(event>=3){pattern='Tuần này có nhiều quyết định phát sinh vì biến động/sự kiện.';next='Trước khi hành động theo tin mới, phân loại lại Reliability và Materiality để giảm phản ứng cảm xúc.'}else if(post>=2){const low=recent.filter(r=>r.decision_stage==='POST_SELL'&&Number(r.decision_quality||0)<60).length;if(low>=2){pattern='Nhiều review sau bán có Decision Quality thấp; lỗi đang nằm ở quy trình hơn là outcome.';next='Không tăng size tuần tới. Chọn 1 rule bị phá nhiều nhất và siết lại trước khi giao dịch.'}}await save('WEEKLY_REVIEW','WEEKLY_PATTERN',{decision_count:recent.length},{prebuy:pre,postsell:post,event},next,80);$('#rCount').textContent=recent.length;$('#rPrebuy').textContent=pre;$('#rPostsell').textContent=post;$('#rPattern').textContent=pattern;$('#rAction').textContent=next;show()}
if(await ensureAuth()){const run=$('#run');if(run)run.onclick=async()=>{setMsg('Đang phân tích...');try{if(stage==='PRE_BUY')await runPrebuy();else if(stage==='HOLDING')await runHolding();else if(stage==='EVENT')await runEvent();else if(stage==='POST_SELL')await runPostSell();else if(stage==='WEEKLY_REVIEW')await runWeekly();setMsg('Đã lưu vào lịch sử quyết định của anh/chị.')}catch(e){console.error(e);setMsg('Có lỗi khi xử lý. Vui lòng thử lại.')}}}