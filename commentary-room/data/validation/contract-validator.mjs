const QUALITY=new Set(['EXACT','DERIVED_EXACT','APPROXIMATE','UNAVAILABLE']);const FRESH=new Set(['FRESH','AGING','STALE','INVALID','CLOSED_SNAPSHOT']);
const TOP=['contract_version','meta','session','indexes','breadth','liquidity','sectors','stocks','technical','derivatives','contributions','context','freshness','provenance'];
function walk(v,path,errors){if(!v||typeof v!=='object')return;if('value'in v&&'provenance'in v){const p=v.provenance;if(!QUALITY.has(p?.quality))errors.push(`${path}.provenance.quality`);if(!FRESH.has(p?.freshness_status))errors.push(`${path}.provenance.freshness_status`);for(const k of ['source','source_path','source_timestamp','received_at','freshness_seconds','derived_from'])if(!(k in p))errors.push(`${path}.provenance.${k}`);return;}if(Array.isArray(v)){v.forEach((x,i)=>walk(x,`${path}[${i}]`,errors));return;}for(const[k,x]of Object.entries(v))walk(x,path?`${path}.${k}`:k,errors);}
export function validateCommentaryPacket(packet){const errors=[];if(packet?.contract_version!=='1.0')errors.push('contract_version');for(const k of TOP)if(!(k in (packet||{})))errors.push(k);if(!packet?.meta?.packet_id)errors.push('meta.packet_id');if(!packet?.session?.phase)errors.push('session.phase');if(!packet?.indexes?.vnindex)errors.push('indexes.vnindex');walk(packet,'',errors);return{valid:errors.length===0,errors:[...new Set(errors)]};}
