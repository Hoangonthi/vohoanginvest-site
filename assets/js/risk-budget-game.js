import {trackTool,smartBack} from './tool-events.js';
const app=document.querySelector('#app');document.querySelector('#back').onclick=smartBack;trackTool('RISK_BUDGET','VIEW');trackTool('RISK_BUDGET','START');
app.innerHTML=`<div class="facts"><div class="fact"><span>Vốn tài khoản</span><strong><input id="capital" type="number" min="1000000" step="1000000" value="500000000"></strong></div><div class="fact"><span>Lỗ tối đa / lệnh (%)</span><strong><input id="risk" type="number" min="0.1" max="20" step="0.1" value="1.5"></strong></div><div class="fact"><span>Số lệnh cùng rủi ro</span><strong><input id="positions" type="number" min="1" max="20" step="1" value="4"></strong></div></div><div class="question"><h3>Drawdown tối đa anh/chị chấp nhận cho toàn tài khoản?</h3><select id="dd"><option value="5">5%</option><option value="10" selected>10%</option><option value="15">15%</option><option value="20">20%</option><option value="30">30%</option></select></div><div class="actions"><button class="btn primary" id="calc">Chạy mô phỏng</button></div><div id="result"></div><p class="note">Mô phỏng giáo dục, không phải khuyến nghị phân bổ vốn hay mức stop-loss cụ thể.</p>`;
document.querySelector('#calc').onclick=async()=>{const capital=+document.querySelector('#capital').value;const risk=+document.querySelector('#risk').value;const positions=+document.querySelector('#positions').value;const dd=+document.querySelector('#dd').value;if(!(capital>0&&risk>0&&positions>0)){alert('Nhập số hợp lệ nhé.');return}const perTrade=capital*risk/100,total=perTrade*positions,totalPct=risk*positions,room=dd-totalPct;let code,title;if(totalPct<=dd*0.5){code='CONSERVATIVE';title='Ngân sách rủi ro còn dư địa'}else if(totalPct<=dd){code='BALANCED';title='Rủi ro đang gần giới hạn chịu đựng'}else{code='OVER_BUDGET';title='Đang vượt ngân sách rủi ro'}await trackTool('RISK_BUDGET','COMPLETE',{resultCode:code,score:Math.max(0,Math.round(100-totalPct/dd*100)),metadata:{risk_per_trade:risk,positions,drawdown_limit:dd,total_risk_pct:+totalPct.toFixed(2)}});document.querySelector('#result').innerHTML=`<section class="result"><h2>${title}</h2><div class="facts"><div class="fact">Rủi ro / lệnh<strong>${perTrade.toLocaleString('vi-VN')} đ</strong></div><div class="fact">Tổng rủi ro đang mở<strong>${totalPct.toFixed(1)}%</strong></div><div class="fact">So với ngưỡng DD<strong>${room>=0?'Còn '+room.toFixed(1)+'%':'Vượt '+Math.abs(room).toFixed(1)+'%'}</strong></div></div><div class="cta">${code==='OVER_BUDGET'?'Nếu nhiều vị thế cùng đi ngược, tài khoản có thể chạm drawdown nhanh hơn khả năng xử lý. Ưu tiên giảm rủi ro đồng thời thay vì chỉ nhìn từng lệnh riêng lẻ.':code==='BALANCED'?'Mức hiện tại chưa vượt ngưỡng, nhưng ít dư địa cho sai số, gap giá hoặc các vị thế tương quan cùng giảm.':'Cấu trúc đang có khoảng đệm tương đối tốt so với giới hạn drawdown đã chọn.'}</div><div class="actions"><a class="btn primary" href="trade-autopsy.html">Mổ xẻ một lệnh thua</a><a class="btn" href="./#assessment">Làm Assessment 17 câu</a></div></section>`};